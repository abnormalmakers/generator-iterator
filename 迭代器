什么是迭代器？
    迭代器是指可用iter（可迭代对象）函数返回的对象实例（）
    迭代器可用next(it)函数获取可迭代对象数据
    任何实现了__iter__和__next__()（python2中实现next()）方法的对象都是迭代器，__iter__返回迭代器自身，__next__返回容器中的下一个值，
    如果容器中没有更多元素了，则抛出StopIteration异常

    迭代器是由可迭代对象生成的，迭代器可以访问可迭代对象的数据

迭代器函数？
    iter(iterable)从可迭代对象中返回一个迭代器，iterable必须是一个能提供迭代器的可迭代对象
    next（iterator）从迭代器iterator中获取下一条记录，如果无法获取下一条记录，则触发StopIteration异常

说明：
    迭代器是访问可迭代对象的一种方式
    迭代器只能向前取值，不能后退
    用iter函数可返回一个可迭代对象的迭代器
