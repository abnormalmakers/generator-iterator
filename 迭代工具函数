迭代工具函数的作用：
    生成一个个性化的可迭代对象


函数：
    zip(iter1,[iter2,iter3....]),返回一个zip对象，此对象用于生成一个元组，元组的个数

    enumerate(iterable,[start])
        生成带索引的枚举对象，返回迭代类型为索引-值对（index,value），默认索引从0开始，也可以用start绑定

        